# Google Drive 연동 설정 가이드

관리자 페이지에서 Google Drive와 연동하여 갤러리 파일을 바로 업로드할 수 있도록 설정하는 방법입니다.

## ✅ 현재 설정 상태

- **API 키**: ✅ 설정 완료 (`AIzaSyAR0pKih7x68qN5m0TKL8P34DAjh6VTN-o`)
- **폴더 ID**: ✅ 설정 완료
  - 교회행사: `1LjNa6LTwdZrGFcsDjereCDsP7QBZVryt`
  - 유치부: `1vhJxT8UCFbwOWQ4LOCwYgitndwFs3yvl`
  - 청년부: `1JxUQ4AoFu1J3YGL_92kpKsyzjVXrQmXu`
- **OAuth 클라이언트 ID**: ⚠️ 설정 필요 (아래 가이드 참고)

## 📋 사전 준비사항

1. Google 계정
2. Google Cloud Console 접근 권한

## 🔧 설정 단계

### 1단계: Google Cloud Console 프로젝트 생성

1. [Google Cloud Console](https://console.cloud.google.com/)에 접속
2. 상단의 프로젝트 선택 메뉴에서 **"새 프로젝트"** 클릭
3. 프로젝트 이름 입력 (예: "Bucheon Church Media")
4. **"만들기"** 클릭

### 2단계: Google Drive API 활성화

1. 왼쪽 메뉴에서 **"API 및 서비스"** > **"라이브러리"** 선택
2. 검색창에 **"Google Drive API"** 입력
3. **"Google Drive API"** 선택 후 **"사용"** 클릭
4. 검색창에 **"Google Picker API"** 입력
5. **"Google Picker API"** 선택 후 **"사용"** 클릭

### 3단계: OAuth 동의 화면 설정

1. 왼쪽 메뉴에서 **"API 및 서비스"** > **"OAuth 동의 화면"** 선택
2. 사용자 유형 선택: **"외부"** 선택 후 **"만들기"**
3. 앱 정보 입력:
   - 앱 이름: "부천교회 미디어 관리"
   - 사용자 지원 이메일: 본인 이메일
   - 앱 로고: (선택사항)
4. **"저장 후 다음"** 클릭
5. 범위(Scopes) 설정:
   - **"범위 추가 또는 삭제"** 클릭
   - `https://www.googleapis.com/auth/drive.readonly` 선택
   - **"업데이트"** 클릭
   - **"저장 후 다음"** 클릭
6. 테스트 사용자 추가:
   - **"사용자 추가"** 클릭
   - 관리자 이메일 주소 입력
   - **"저장 후 다음"** 클릭
7. 요약 화면에서 **"대시보드로 돌아가기"** 클릭

### 4단계: OAuth 2.0 클라이언트 ID 생성

1. 왼쪽 메뉴에서 **"API 및 서비스"** > **"사용자 인증 정보"** 선택
2. 상단의 **"+ 사용자 인증 정보 만들기"** > **"OAuth 클라이언트 ID"** 선택
3. 애플리케이션 유형: **"웹 애플리케이션"** 선택
4. 이름: "부천교회 미디어 관리 웹 클라이언트"
5. 승인된 자바스크립트 원본:
   - `http://localhost:3000` (로컬 개발용)
   - 실제 도메인 추가 (예: `https://yourdomain.com`)
6. 승인된 리디렉션 URI:
   - `http://localhost:3000` (로컬 개발용)
   - 실제 도메인 추가 (예: `https://yourdomain.com`)
7. **"만들기"** 클릭
8. **클라이언트 ID**를 복사해두세요 (나중에 필요합니다)

### 5단계: API 키 생성

1. **"API 및 서비스"** > **"사용자 인증 정보"** 페이지에서
2. **"+ 사용자 인증 정보 만들기"** > **"API 키"** 선택
3. 생성된 API 키를 복사해두세요
4. (선택) API 키 제한 설정:
   - 생성된 API 키 옆의 연필 아이콘 클릭
   - **"API 제한사항"** 선택
   - **"키 제한"** 선택
   - **"Google Picker API"**와 **"Google Drive API"** 선택
   - **"저장"** 클릭

### 6단계: 코드에 OAuth 클라이언트 ID 적용

⚠️ **중요**: API 키와 폴더 ID는 이미 설정되어 있습니다. OAuth 클라이언트 ID만 추가로 설정하면 됩니다.

1. `admin/media-upload.html` 파일을 엽니다
2. 파일 상단의 `<script>` 태그 내에서 다음 부분을 찾습니다:

```javascript
const GOOGLE_CLIENT_ID = 'YOUR_GOOGLE_CLIENT_ID';
```

3. 위에서 복사한 OAuth 클라이언트 ID로 교체합니다:

```javascript
const GOOGLE_CLIENT_ID = '여기에_클라이언트_ID_붙여넣기';
```

4. `admin/content-manage.html` 파일도 동일하게 수정합니다
5. 파일을 저장합니다

**참고**: 
- API 키는 이미 `AIzaSyAR0pKih7x68qN5m0TKL8P34DAjh6VTN-o`로 설정되어 있습니다.
- 폴더 ID도 자동으로 매핑되어 있어, 카테고리 선택 시 해당 폴더가 자동으로 열립니다.

## ✅ 테스트

1. 관리자 페이지(`admin/media-upload.html`)에 접속
2. **"Google Drive"** 카드를 클릭
3. Google 계정 로그인 (처음 한 번만 필요)
4. Google Drive에서 파일 선택
5. 선택한 파일이 자동으로 다운로드되어 업로드 준비 상태가 됩니다
6. 카테고리 선택 후 **"업로드 시작"** 버튼 클릭

## 🔒 보안 주의사항

1. **API 키 노출 주의**: 
   - API 키는 클라이언트 측 코드에 포함되므로, 반드시 API 키 제한을 설정하세요
   - Google Cloud Console에서 API 키 제한을 설정하면 특정 API만 사용 가능하도록 제한할 수 있습니다

2. **OAuth 클라이언트 ID**:
   - 승인된 자바스크립트 원본에 실제 도메인을 반드시 추가하세요
   - 로컬 개발 시에만 `localhost`를 사용하세요

3. **프로덕션 환경**:
   - 프로덕션 배포 전에 OAuth 동의 화면을 검토 및 게시해야 할 수 있습니다
   - 테스트 사용자로는 제한된 사용자만 접근 가능합니다

## 🐛 문제 해결

### "API 키 설정 필요" 메시지가 나타나는 경우
- `media-upload.html` 파일에서 `GOOGLE_API_KEY`와 `GOOGLE_CLIENT_ID`가 올바르게 설정되었는지 확인하세요

### "OAuth 오류"가 발생하는 경우
- OAuth 동의 화면에서 테스트 사용자로 본인 이메일이 추가되었는지 확인하세요
- 승인된 자바스크립트 원본에 현재 도메인이 추가되었는지 확인하세요

### 파일 다운로드가 실패하는 경우
- Google Drive API가 활성화되었는지 확인하세요
- OAuth 범위에 `drive.readonly`가 포함되었는지 확인하세요

## 📞 추가 도움말

- [Google Picker API 문서](https://developers.google.com/picker)
- [Google Drive API 문서](https://developers.google.com/drive)
- [OAuth 2.0 설정 가이드](https://developers.google.com/identity/protocols/oauth2)
